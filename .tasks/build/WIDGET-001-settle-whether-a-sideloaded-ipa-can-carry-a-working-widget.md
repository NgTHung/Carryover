---
id: "WIDGET-001"
title: "Settle whether a sideloaded IPA can carry a working widget"
status: Done
priority: "High"
type: "Spike"
milestone: "0.1.0"
depends_on: ["BUILD-001"]
risk: "High"
impact: "Gates the roadmap. The spec puts a widget at stage 6 and assumes it is reachable. If a free Apple account cannot provision the App Group the widget needs, the paid membership moves earlier and the stage order changes."
tags: ["widget", "ios", "spike"]
last_updated: 2026-09-02
---

## Summary

A widget extension and the data it renders fail for different reasons, so the spike separates them. The widget component falls back to fixture values and draws even when the app has never sent it anything, which answers whether a sideloaded IPA carries an extension at all. A button on the spike screen then calls updateSnapshot with a random value and reloads, which answers whether the App Group data path works on this signing setup. Errors are surfaced on screen because there is no simulator and no console to read. The workflow also dumps every appex in the packaged IPA and every entitlements file, so an extension that never reached the bundle is distinguishable from one that failed to sign.

## Acceptance Criteria

- [x] The widget renders fixture values without the app having pushed anything, so an install can be confirmed independently of the data path.
- [x] The spike screen surfaces the updateSnapshot error text on device when the push fails.
- [x] The workflow step summary lists every appex in the packaged IPA and every entitlements file generated by prebuild.
- [x] A no-widget control variant builds from the same commit.
- [x] docs/build/widget-sideload-result.md is filled in with both answers, the signing tool, the account type, and any error text.
- [x] The roadmap consequence is written down, in particular whether the paid membership moves earlier than stage 6.
